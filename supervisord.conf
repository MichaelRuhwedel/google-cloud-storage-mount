[supervisord]
nodaemon=true

[program:gcsfuse]
command=gcsfuse $BUCKET /mnt/bucket >> /var/log/debug
autorestart=true
priority=1

[program:mkdir]
command=mkdir -p /mnt/bucket/appengine.googleapis.com/request_log/`date +"%%Y"`/`date +"%%m"`/`date +"%%d"` >> /var/log/debug
autorestart=false
priority=2

[program:rsync]
command=rsync --ignore-existing -ra /mnt/bucket /mnt/logfiles_incoming
autorestart=false
priority=3
